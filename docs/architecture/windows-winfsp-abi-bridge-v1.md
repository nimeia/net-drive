# Iter 27 — WinFsp ABI bridge / dispatcher service loop v1

This iteration moves `dispatcher-v1` beyond API probing by adding a Go-side ABI bridge, a dispatcher service-loop scaffold, warmup logic, and runtime/diagnostics visibility for callback/service readiness.
